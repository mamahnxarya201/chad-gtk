{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build (Meson)",
      "type": "shell",
      "command": "meson setup build --reconfigure --buildtype=debug && ninja -C build",
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
